{
  "name": "@claricore/webhook-gateway",
  "version": "0.4.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "node -e \"console.log('lint @claricore/webhook-gateway')\"",
    "test": "node -e \"console.log('test @claricore/webhook-gateway')\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@claricore/config": "workspace:*",
    "@claricore/db": "workspace:*",
    "@claricore/observability": "workspace:*",
    "@claricore/queue": "workspace:*",
    "@claricore/ui-contracts": "workspace:*",
    "zod": "^3.23.8"
  }
}
