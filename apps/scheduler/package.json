{
  "name": "@claricore/scheduler",
  "version": "0.4.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "node -e \"console.log('lint @claricore/scheduler')\"",
    "test": "node -e \"console.log('test @claricore/scheduler')\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@claricore/db": "workspace:*",
    "@claricore/observability": "workspace:*",
    "@claricore/queue": "workspace:*",
    "node-cron": "^3.0.3"
  }
}
